# Validate public files before starting or reloading the configured sites.
[Service]
ExecStartPre=/usr/local/bin/check-publication --caddy /etc/caddy/Caddyfile
ExecReload=
ExecReload=/usr/local/bin/reload-public-sites
